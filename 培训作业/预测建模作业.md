#### 作业要求![[1720940654570.png]]

#### 实现过程
- **加载数据集**
```R
library(data.table)
library(forecast)
flu <- fread("french_flu.csv")
flu[, flu.rate := as.numeric(TauxGrippe)]
head(flu, n=2)
```
- **数据预处理**
	- 检查缺失值
	- Data类型转化
	- 将数据按照时间排序
```R
# 检查缺失值
missing_rate <- nrow(flu[is.na(flu.rate)])/nrow(flu)
missing_regions <- unique(flu[is.na(flu.rate)]$region_name)
# 时间格式转化
flu[, year := as.numeric(substr(week, 1, 4))]
flu[, wk := as.numeric(substr(week, 5, 6))]
flu[, date := as.Date(paste0(as.character(flu$week), "1"), "%Y%U%u")]
head(flu)
# 排序
paris.flu <- flu[region_name == "ILE-DE-FRANCE"]
paris.flu <- paris.flu[order(date, decreasing = FALSE)]
paris.flu[-1*(5:518), .(Id, week, date, flu.rate)]
```
- **数据可视化分析**
	- 查看是否由趋势性、季节性
```R
year_week_counts <- paris.flu[, .N, year]
week_year_counts <- paris.flu[, .N, wk][c(1, 2, 51, 52, 53)]
plot(paris.flu$date, paris.flu$flu.rate, type="l", xlab="Date", ylab="Flu rate")

paris.flu <- paris.flu[wk != 53]
```
- **对数变换和标准化**
```R
paris.flu[, log_flu_rate := log(flu.rate + 1)]
paris.flu[, scaled_log_flu_rate := scale(log_flu_rate)]

plot(paris.flu$date, paris.flu$scaled_log_flu_rate, type="l", xlab="Date", ylab="Scaled Log Flu Rate")
```
- **差分处理**
```R
par(mfrow=c(1,2))
acf(paris.flu$scaled_log_flu_rate, lag.max=104 )
acf(diff(paris.flu$scaled_log_flu_rate, 52), lag.max = 104)

# 季节性差分
seasonal_diff <- diff(paris.flu$scaled_log_flu_rate, 52)

# 一阶差分
differenced_data <- diff(seasonal_diff, 1)
plot(differenced_data, type="l", main="Differenced Scaled Log Flu Rate")
```
- **模型选择**
```R
par(mfrow=c(2,1))
acf(differenced_data, lag.max = 104)
pacf(differenced_data, lag.max=104)
```
- 建立SARIMA模型
```R
library(forecast)
first.fit.size <- 104
h <- 2
n <- nrow(paris.flu) - h - first.fit.size

# 使用ARIMA模型拟合数据
first.fit <- arima(paris.flu$scaled_log_flu_rate[1:first.fit.size], order=c(2,1,0),
                   seasonal = list(order=c(0,1,0), period=52))
first.order <- arimaorder(first.fit)
summary(first.fit)
```
- **向前滚动拟合并预测**
```R
fit.preds <- array(0, dim=c(n, h))
fit.coefs <- array(0, dim=c(n, length(first.fit$coef)))

for(i in (1+ first.fit.size):(nrow(paris.flu) - h)){
  data.to.fit <- paris.flu[1:i]
  fit <- arima(data.to.fit$scaled_log_flu_rate, order=first.order[1:3],
               seasonal=list(order=c(0,1,0), period=52))
  fit.preds[i-first.fit.size,] <- forecast(fit, h=2)$mean
  fit.coefs[i-first.fit.size,] <- fit$coef
}
```
- **反变换**和绘图比较实际值和预测值
```R
mean_log_flu_rate <- mean(paris.flu$log_flu_rate)
sd_log_flu_rate <- sd(paris.flu$log_flu_rate)

predictions <- exp(fit.preds * sd_log_flu_rate + mean_log_flu_rate) - 1

ylim <- range(paris.flu$flu.rate[300:400], predictions[,h][(300-h):(400-h)])
par(mfrow=c(1,1))
plot(paris.flu$date[300:400], paris.flu$flu.rate[300:400],
     ylim=ylim, cex=0.8,
     main="Actual and Predicted Flu Rate",
     xlab= "Date", ylab="Flu Rate")
lines(paris.flu$date[300:400], predictions[,h][(300-h):(400-h)], col=2, lty=2, lwd=2)
```

#### 作业结果
![[plot.png]]
可以看到在数据平稳时预测拟合效果较好，但在数据上升期有延后现象，并且出现异常大的数据