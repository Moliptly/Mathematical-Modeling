- 论文题目有特色
- 数据来源可靠
- 步骤完整
- 论文规范
#### 数据读取
- 语言： R、python、spss、sas、stata
	```R
	# 读取 sites.xlsx 第一个工作表数据
	data <- read.xlsx("sites.xlsx", sheetIndex = 1)
	print(data)
	
	# 读取csv文件
	data <- read.csv("sites.csv", encoding="UTF-8")
	# likes 为 222 的数据
	retval <- subset(data, likes == 222)
	# 写入新的文件
	write.csv(retval,"runoob.csv", row.names = FALSE)
	newdata <- read.csv("runoob.csv")
	print(newdata)
	```
#### 数据变换
- 对数据进行清洗、融合、变换
	- 异方差现象
		- 用对数变换，将原序列变成==方差近似为常数==的序列
		- 数列的值越大，波动越大
		- Taylor公式
	- 时间序列模型
		- 差分变换
		- 对数变换
	
#### 优化函数使用
- 直方图呈现==多峰分布==特征，选用==混合正态分布拟合==
- R语言中nlminb函数（通过最小化目标函数得到估计结果）

#### 统计作图选讲
- 箱线图
	- R语言boxplot（）函数
- 雷达图（蜘蛛图/极坐标图）
	- fmsb包的radarchart（）函数
- 调和曲线（安德鲁斯曲线）
	- 运用三角多项式变换将高维空间的观测样本映射到二维空间平面上的一条曲线
	- 对于聚类分析很有帮助，同类的曲线往往靠近在一起
	- R语言中的MSG包中的andrews_curve()
#### 模型选择
- AIC、BIC模型选择遵从简约原则
- ACF、PACF拖尾、截尾
